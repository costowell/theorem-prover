<toplevel>  ::= <stmt> | <stmt> <binop> <stmt>
<stmt>      ::= "!" <substmt> | <substmt>
<substmt>   ::= "(" <stmt> <binop> <stmt> ")" | <predicate>
<binop>     ::= "&" | "|" | "->" | "<->"
<predicate> ::= [A-z]+
